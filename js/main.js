(() => {
  const copy = {
    zh: {
      'nav.experience': '经历', 'nav.projects': '项目', 'nav.capabilities': '能力', 'nav.education': '教育', 'nav.contact': '联系',
      'hero.eyebrow': 'AI PRODUCT · AGENT · DATA',
      'hero.title': '让 AI 产品<br><span>真正工作。</span>',
      'hero.intro': '你好，我是贺睿 Reina。我把复杂的模型能力，变成清晰、可测量、能增长的用户体验。',
      'hero.work': '查看作品', 'hero.building': 'CURRENTLY BUILDING', 'hero.proof': 'EXPERIENCE ACROSS',
      'hero.location': 'DURHAM · CHINA', 'hero.available': '开放 2027 机会',
      'focus.label': '产品闭环', 'focus.step1': '发现', 'focus.step2': '编排', 'focus.step3': '验证',
      'metrics.apps': 'Agent Apps', 'metrics.appsSub': '两个月上线', 'metrics.wan': '万+', 'metrics.credits': '积分消耗', 'metrics.creditsSub': '验证真实需求',
      'metrics.samples': '评测样本', 'metrics.samplesSub': '图像 / 视频 / 编辑', 'metrics.resolve': '会话解决率', 'metrics.resolveSub': '由 88.3% 提升',
      'experience.title': '做出来，<br><span>再做得更好。</span>', 'experience.intro': '从需求、编排到评测与增长，我负责完整闭环。点击每段经历查看关键成果。',
      'experience.sandRole': 'Agent 产品实习生', 'experience.sandArea': '视频生成 · Agent Apps', 'experience.sandSummary': '负责 Agent App 从场景发现、方案设计、Vibe Coding 到上线验证。',
      'experience.sandA1': '上线 16 个 App，筛出 4 个高潜场景', 'experience.sandA2': '用 500+ 样本提升核心场景完片率', 'experience.sandA3': '提升画布复用率与新用户转化',
      'experience.baidu': '百度', 'experience.baiduRole': 'AI 产品实习生', 'experience.baiduArea': '智能客服 · Agent 平台', 'experience.baiduSummary': '负责企业级 Agent 平台体验与金融客服多 Agent 方案。',
      'experience.baiduA1': '覆盖贷款、还款、理财核心咨询', 'experience.baiduA2': '通过路由、RAG 与兜底提升解决率',
      'experience.jd': '京东', 'experience.jdRole': '数据分析实习生', 'experience.jdArea': '人才数据 · 指标体系', 'experience.jdSummary': '整合多源人才数据，建立统一口径与周期性分析。',
      'experience.jdA1': '打通岗位、绩效、潜力与培养记录', 'experience.jdA2': '用行为与问卷数据形成问题归因闭环',
      'projects.title': '把能力，<br><span>变成任务。</span>', 'projects.intro': '少讲功能，多看用户最终能完成什么。', 'projects.owner': '项目负责人',
      'projects.featuredLabel': 'AI 广告创作', 'projects.featuredTitle': '从商品素材到<br>可编辑广告成片', 'projects.featuredCopy': '输入商品与营销诉求，Agent 自动编排脚本、画面与生成工具，并在 Canvas 中继续编辑。',
      'projects.users': '用户 / 商家', 'projects.complete': '任务完成率', 'projects.open': '打开 Vidmuse APP',
      'projects.adLabel': '从数据到决策', 'projects.adTitle': '广告数据分析<br>Agent', 'projects.adCopy': '自动识别指标、定位异常、归因效果并生成策略建议。',
      'projects.priceLabel': '从模型到解释', 'projects.priceTitle': '跨境定价预测<br>与弹性分析', 'projects.priceCopy': '比较 RF、XGBoost 与 LSTM，并用 SHAP 解释核心定价因子。',
      'capabilities.title': '产品判断力，<br><span>数据与工程语言。</span>', 'capabilities.ai': 'AI 产品', 'capabilities.data': '数据科学', 'capabilities.coding': 'AI Coding', 'capabilities.design': '产品设计',
      'education.title': '商业理解，<br><span>数据验证。</span>', 'education.durham': '杜伦大学', 'education.master': '数据科学硕士', 'education.csuft': '中南林业科技大学',
      'education.bachelor': '工商管理（人力资源管理）· 本科', 'education.awards': '国家二等奖学金 · “互联网+”省级三等奖',
      'contact.label': '一起做点真正有用的事', 'contact.title': '有值得落地的<br><span>AI 问题？</span>', 'contact.action': '和我聊聊', 'footer.top': '返回顶部 ↑'
    },
    en: {
      'nav.experience': 'Experience', 'nav.projects': 'Work', 'nav.capabilities': 'Toolkit', 'nav.education': 'Education', 'nav.contact': 'Contact',
      'hero.eyebrow': 'AI PRODUCT · AGENT · DATA',
      'hero.title': 'AI products that<br><span>actually work.</span>',
      'hero.intro': 'Hi, I’m Reina He. I turn complex model capabilities into clear, measurable products people want to use.',
      'hero.work': 'See my work', 'hero.building': 'CURRENTLY BUILDING', 'hero.proof': 'EXPERIENCE ACROSS',
      'hero.location': 'DURHAM · CHINA', 'hero.available': 'OPEN TO 2027',
      'focus.label': 'PRODUCT LOOP', 'focus.step1': 'Discover', 'focus.step2': 'Build', 'focus.step3': 'Validate',
      'metrics.apps': 'Agent Apps', 'metrics.appsSub': 'shipped in 2 months', 'metrics.wan': '0K+', 'metrics.credits': 'Credits consumed', 'metrics.creditsSub': 'real demand validated',
      'metrics.samples': 'Evaluation samples', 'metrics.samplesSub': 'image / video / edit', 'metrics.resolve': 'Resolution rate', 'metrics.resolveSub': 'up from 88.3%',
      'experience.title': 'Ship it.<br><span>Then sharpen it.</span>', 'experience.intro': 'From discovery and orchestration to evaluation and growth. Open each role for the outcomes.',
      'experience.sandRole': 'Agent Product Intern', 'experience.sandArea': 'Video Generation · Agent Apps', 'experience.sandSummary': 'Owned Agent Apps from opportunity discovery and solution design to vibe coding and launch validation.',
      'experience.sandA1': 'Shipped 16 apps and identified 4 high-potential use cases', 'experience.sandA2': 'Used 500+ samples to improve core completion rate', 'experience.sandA3': 'Lifted canvas reuse and new-user conversion',
      'experience.baidu': 'Baidu', 'experience.baiduRole': 'AI Product Intern', 'experience.baiduArea': 'Customer Service · Agent Platform', 'experience.baiduSummary': 'Improved an enterprise Agent platform and designed multi-agent flows for financial services.',
      'experience.baiduA1': 'Covered core loan, repayment and wealth queries', 'experience.baiduA2': 'Improved resolution with routing, RAG and fallbacks',
      'experience.jd': 'JD.com', 'experience.jdRole': 'Data Analyst Intern', 'experience.jdArea': 'People Analytics · Metrics', 'experience.jdSummary': 'Unified multi-source people data into a consistent measurement and analysis system.',
      'experience.jdA1': 'Connected role, performance, potential and learning data', 'experience.jdA2': 'Closed the insight loop with behavior and survey data',
      'projects.title': 'Capabilities,<br><span>made actionable.</span>', 'projects.intro': 'Less about feature lists. More about what users can finish.', 'projects.owner': 'PRODUCT OWNER',
      'projects.featuredLabel': 'AI AD CREATION', 'projects.featuredTitle': 'From product assets to<br>editable video ads', 'projects.featuredCopy': 'The Agent orchestrates scripts, scenes and generation tools, then hands the result to Canvas for continued editing.',
      'projects.users': 'users / merchants', 'projects.complete': 'task completion', 'projects.open': 'Open Vidmuse APP',
      'projects.adLabel': 'DATA TO DECISION', 'projects.adTitle': 'Ad Performance<br>Analysis Agent', 'projects.adCopy': 'Recognizes metrics, spots anomalies, explains performance and suggests the next action.',
      'projects.priceLabel': 'MODEL TO EXPLANATION', 'projects.priceTitle': 'Cross-border Pricing<br>& Elasticity', 'projects.priceCopy': 'Compared RF, XGBoost and LSTM, then explained pricing drivers with SHAP.',
      'capabilities.title': 'Product judgment,<br><span>spoken in data & code.</span>', 'capabilities.ai': 'AI Product', 'capabilities.data': 'Data Science', 'capabilities.coding': 'AI Coding', 'capabilities.design': 'Product Design',
      'education.title': 'Business context,<br><span>tested with data.</span>', 'education.durham': 'Durham University', 'education.master': 'MSc Data Science', 'education.csuft': 'Central South University of Forestry & Technology',
      'education.bachelor': 'BBA, Human Resource Management', 'education.awards': 'National Second-Class Scholarship · Provincial Innovation Award',
      'contact.label': 'LET’S BUILD SOMETHING USEFUL', 'contact.title': 'An AI problem<br><span>worth solving?</span>', 'contact.action': 'Let’s talk', 'footer.top': 'Back to top ↑'
    }
  };

  const focusCopy = {
    zh: [
      { value: 'WHY', title: '找到真问题', body: '从用户任务与业务目标出发，不为 AI 而 AI。' },
      { value: 'HOW', title: '设计好系统', body: '让 Agent、Skill、Tool 与 Memory 各司其职。' },
      { value: 'LOOP', title: '用数据迭代', body: '把日志、Bad Case、评测与增长连成闭环。' }
    ],
    en: [
      { value: 'WHY', title: 'Find the real problem', body: 'Start with user jobs and business value—not AI for AI’s sake.' },
      { value: 'HOW', title: 'Design the system', body: 'Give Agents, Skills, Tools and Memory a clear role.' },
      { value: 'LOOP', title: 'Learn with data', body: 'Connect logs, bad cases, evaluation and growth.' }
    ]
  };

  const header = document.getElementById('site-header');
  const progress = document.getElementById('scroll-progress');
  const languageToggle = document.getElementById('language-toggle');
  const year = document.getElementById('year');
  const focusPanel = document.querySelector('.focus-panel');
  const focusValue = document.getElementById('focus-value');
  const focusTitle = document.getElementById('focus-title');
  const focusBody = document.getElementById('focus-copy');
  const focusTabs = [...document.querySelectorAll('.focus-tab')];
  let currentLanguage = 'zh';
  let currentFocus = 0;

  const updateFocus = (index, animate = true) => {
    currentFocus = index;
    const item = focusCopy[currentLanguage][index];
    focusValue.textContent = item.value;
    focusTitle.textContent = item.title;
    focusBody.textContent = item.body;
    focusTabs.forEach((tab, tabIndex) => {
      const active = tabIndex === index;
      tab.classList.toggle('active', active);
      tab.setAttribute('aria-selected', String(active));
    });
    if (animate) {
      focusPanel.classList.remove('is-changing');
      requestAnimationFrame(() => focusPanel.classList.add('is-changing'));
    }
  };

  const applyLanguage = (language) => {
    currentLanguage = language;
    document.documentElement.lang = language === 'zh' ? 'zh-CN' : 'en';
    document.querySelectorAll('[data-i18n]').forEach((element) => {
      const value = copy[language][element.dataset.i18n];
      if (value !== undefined) element.textContent = value;
    });
    document.querySelectorAll('[data-i18n-html]').forEach((element) => {
      const value = copy[language][element.dataset.i18nHtml];
      if (value !== undefined) element.innerHTML = value;
    });
    languageToggle.setAttribute('aria-label', language === 'zh' ? 'Switch to English' : '切换到中文');
    document.title = language === 'zh' ? '贺睿 Reina He｜AI 产品经理' : 'Reina He | AI Product Manager';
    updateFocus(currentFocus, false);
    try { localStorage.setItem('portfolio-language', language); } catch (_) { /* preference is optional */ }
  };

  const updateScroll = () => {
    const y = window.scrollY;
    const max = document.documentElement.scrollHeight - window.innerHeight;
    header?.classList.toggle('scrolled', y > 20);
    if (progress) progress.style.width = `${max > 0 ? (y / max) * 100 : 0}%`;
  };

  const revealObserver = new IntersectionObserver((entries, observer) => {
    entries.forEach((entry) => {
      if (!entry.isIntersecting) return;
      entry.target.classList.add('is-visible');
      observer.unobserve(entry.target);
    });
  }, { threshold: 0.08, rootMargin: '0px 0px -35px' });

  document.querySelectorAll('.reveal').forEach((element, index) => {
    element.style.setProperty('--delay', `${Math.min(index % 4, 3) * 45}ms`);
    revealObserver.observe(element);
  });

  const counterObserver = new IntersectionObserver((entries, observer) => {
    entries.forEach((entry) => {
      if (!entry.isIntersecting) return;
      const element = entry.target;
      const target = Number(element.dataset.count);
      const decimals = Number(element.dataset.decimals || 0);
      const start = performance.now();
      const duration = 1100;
      const tick = (now) => {
        const progressValue = Math.min((now - start) / duration, 1);
        const eased = 1 - Math.pow(1 - progressValue, 3);
        element.textContent = (target * eased).toFixed(decimals);
        if (progressValue < 1) requestAnimationFrame(tick);
      };
      requestAnimationFrame(tick);
      observer.unobserve(element);
    });
  }, { threshold: .65 });
  document.querySelectorAll('.metric-value').forEach((element) => counterObserver.observe(element));

  document.querySelectorAll('a[href^="#"]').forEach((link) => {
    link.addEventListener('click', (event) => {
      const target = document.querySelector(link.getAttribute('href'));
      if (!target) return;
      event.preventDefault();
      target.scrollIntoView({ behavior: 'smooth', block: 'start' });
    });
  });

  focusTabs.forEach((tab) => tab.addEventListener('click', () => updateFocus(Number(tab.dataset.step))));
  languageToggle?.addEventListener('click', () => applyLanguage(currentLanguage === 'zh' ? 'en' : 'zh'));

  document.querySelectorAll('.experience-item').forEach((item) => {
    item.addEventListener('toggle', () => {
      if (!item.open) return;
      document.querySelectorAll('.experience-item').forEach((other) => {
        if (other !== item) other.open = false;
      });
    });
  });

  const sections = [...document.querySelectorAll('main section[id]')];
  const navLinks = [...document.querySelectorAll('.nav-links a')];
  const sectionObserver = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (!entry.isIntersecting) return;
      navLinks.forEach((link) => link.classList.toggle('active', link.getAttribute('href') === `#${entry.target.id}`));
    });
  }, { rootMargin: '-35% 0px -55%', threshold: 0 });
  sections.forEach((section) => sectionObserver.observe(section));

  const finePointer = window.matchMedia('(pointer: fine)').matches;
  const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
  if (finePointer && !reducedMotion) {
    window.addEventListener('pointermove', (event) => {
      document.documentElement.style.setProperty('--mx', `${event.clientX}px`);
      document.documentElement.style.setProperty('--my', `${event.clientY}px`);
    }, { passive: true });

    document.querySelectorAll('[data-tilt]').forEach((card) => {
      card.addEventListener('pointermove', (event) => {
        const rect = card.getBoundingClientRect();
        const x = (event.clientX - rect.left) / rect.width - .5;
        const y = (event.clientY - rect.top) / rect.height - .5;
        card.style.transform = `perspective(900px) rotateX(${y * -3.5}deg) rotateY(${x * 4.5}deg) translateY(-2px)`;
      });
      card.addEventListener('pointerleave', () => { card.style.transform = ''; });
    });

    document.querySelectorAll('.magnetic').forEach((element) => {
      element.addEventListener('pointermove', (event) => {
        const rect = element.getBoundingClientRect();
        const x = (event.clientX - rect.left - rect.width / 2) * .08;
        const y = (event.clientY - rect.top - rect.height / 2) * .12;
        element.style.transform = `translate(${x}px, ${y}px)`;
      });
      element.addEventListener('pointerleave', () => { element.style.transform = ''; });
    });
  }

  let savedLanguage = 'zh';
  try { savedLanguage = localStorage.getItem('portfolio-language') || 'zh'; } catch (_) { /* preference is optional */ }
  applyLanguage(savedLanguage === 'en' ? 'en' : 'zh');
  updateFocus(0, false);
  if (year) year.textContent = String(new Date().getFullYear());
  window.addEventListener('scroll', updateScroll, { passive: true });
  updateScroll();
})();
